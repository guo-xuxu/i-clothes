# 2026-08-25 工作日志

## 知识库文档从三元组列表改为真实原文（用户纠正）

- 用户纠正：之前把穿搭知识写成了「实体→关系→目标」三元组列表，用户要求的是**自然语言原文**（供 graph_builder 用 DeepSeek 抽取三元组），不要预抽取。
- 用户选择来源：**网上搜真实原文**（非原创改写）。
- 执行：逐维度 WebSearch 搜真实中文穿搭文章 → 整理正文成 md（标注来源、去掉图片与营销话术）→ 覆盖旧三元组文档。
- 结果：9 维度 × 5 篇 = 45 篇原文全部完成，均为自然语言原文 + 标注来源。
- 主要来源站点：网易、搜狐、今日头条、微博、伊的家、勤学教育网、穿针引线学院、什么值得买、以及繁体中文站（gojistudios 香港、faqs.tw、uptogo、tyaward、abcfashion 等）；英文 dress code 内容（MeasureGauge/Blinkscoop）综合整理为中文。
- 清理：删除 3 个旧三元组残留（body-shape/五大体型分类、fabric/常见面料选择保养、fabric/面料肌理质感），保证每维度正好 5 篇。
- 注意：这些文档是工作区未提交状态（在 feat/rag-on-springboot 分支），尚未 git 提交。
- 待办：下一步按接口驱动计划 A1，写 app/knowledge/build/ 的 import_docs + 三元组抽取流水线，让 45 篇原文跑起来建图。

## 文本切块实现（用户要求）

- 用户进一步澄清"扒原文"要**大幅保留原文**（WebFetch 抓全文、保留原句、只删图片广告），已重写网易体型篇为样板。
- 随后用户转向写代码：要求 build 目录写文本切块 + knowledge 写 config 控制切分大小和 overlap。
- 完成：
  - `app/knowledge/config.py`：`ChunkConfig`(frozen dataclass)，`chunk_size=500`、`chunk_overlap=50`、`separators`(段落→句号→逗号优先级)；`__post_init__` 校验（size>0、0<=overlap<size）。
  - `app/knowledge/build/text_chunk.py`：`chunk_text(text, config)` 滑动窗口切块 + 句子边界回退（回退上限 30%），返回 `TextChunk(text,index,start,end)`；空文本返回空列表。
  - 删除拼写错误的空壳 `text_chuck.py`（chuck→chunk）。
- 验证通过：540 字→19 块、overlap 生效、句子边界断开、参数校验报错正常。
- 现有骨架：`document_reader.py`（DocumentReader 扫描 docs 推断维度）已实现；`retrieve/{graph_store,vector_store,retriever}` 仍是 docstring 空壳。
