# 2026-08-23 工作日志

## i-clothes 项目代码理解 + PRD 综合评估

- 完成全量代码阅读：Vue3 前端（6 文件）、Java 业务后端（30 主类 + 11 测试类）、Python Agent（FastAPI + LangGraph，4 节点）、PG/Redis 基础设施、六层文档体系。
- 产出《docs/代码与PRD综合评估.md》：PRD 逐项对照、工程质量评估、软件开发流程、模块开发流程、版本路线图。
- 关键结论：
  - P0 四核心功能全部完成；双服务架构落地但 **PRD 勾选框未更新**；部署未做（最大缺口）。
  - P1 DeepSeek 已用于闲聊/推荐，但"单品识别"节点未实现；生图未做。
  - 工程债：图片 base64 永久落 JSONB 无清理；会话锁 TTL 5s 不续期；无用户体系；Java IT 依赖真实 PG。
  - RAG 规划已定（~2.5 天），未排期。
- 已产 3 张可视化图：系统架构图、消息完整流转链路、开发流程与版本路线图。

## 删除过时接口 /api/recommend

- 删除：Java `RecommendController.java` + `RecommendControllerTest.java`、前端孤儿文件 `frontend/app.js` + `style.css`（旧版上传页唯一客户端）。
- 同步：`docs/openapi-contract.json`（移除路径与 schema，JSON 校验通过）、`ChatController` 注释、CHANGELOG、开发指南目录描述、PRD 评估文档遗留项标记已移除。
- 验证：`mvn test` 39 用例全绿（BUILD SUCCESS）；注意本机 Maven 脚本损坏，需用 `java -Dmaven.home=... -Dclassworlds.conf=... -cp boot/plexus-classworlds-*.jar org.codehaus.plexus.classworlds.launcher.Launcher` 直调。
- 遗留待办：`app/graph/workflow.py` 中 `run_recommendation()`（兼容旧接口）仍被 `test/test_workflow.py` 使用，未动。

## Docker 部署咨询 + 傻瓜操作手册

- 用户对 Docker 仅"听过概念没用过"，要一份零理解成本、照抄即可的部署手册。
- 产出 `docs/部署操作手册.md`（v1.0）：装 Docker → 本机构建 jar/dist → 上传 → 填 .env 密钥 → compose up → 验证 → 日常速查表 + FAQ。
- 关键约束已写入手册：Java Dockerfile 非多阶段（需本机先 `mvn package`），前端需先 `npm run build`；`.env` 传密钥；pgdata 卷持久化。
- 用户可能的下一步：把 Dockerfile 改多阶段构建（服务器一键全自动，免第 2 步本机构建）。

## 首次 git 提交（feat/knowledge-graph-rag 分支）

- 提交成功：`d837b95` "feat: i-clothes MVP 双服务架构（首次提交）"，98 文件 13602 行；`.workbuddy/` 已排除未提交。
- 踩坑记录：WorkBuddy 沙箱隔离会丢弃 `git commit` 对 `.git/refs/` 的写入（对象写入成功但分支 ref 文件不落盘，commit 后 `git log` 仍报"无提交"）。修复：沙箱外（dangerouslyDisableSandbox）`mkdir -p .git/refs/heads/feat` 后 `printf '<完整40位sha1>\n' > ref文件`。注意 ref 必须存完整 40 位 SHA-1（短 hash 会报 badRefContent），分支名含 `/` 需先建子目录。
- 未 push（用户未要求），分支无 upstream。

## 知识图谱 + 向量 RAG：spec 与实施计划（用户确认"完整 RAG + 先出计划"）

- 用户选择：完整 RAG（图谱+向量混合，M0-M3）+ 先出计划再写码。
- 产出两份文档（未写码，等用户确认后逐步实现）：
  - `docs/superpowers/specs/2026-08-23-knowledge-graph-rag-design.md`：对外 3 端点（POST/GET /api/knowledge/documents + 列表）、对内 POST /api/agent/knowledge/build（同步）、Python knowledge/ 包 7 模块、V2__knowledge.sql 两表、retrieve_context 节点插入位置、失败模式、验收标准。
  - `docs/superpowers/plans/2026-08-23-knowledge-graph-rag.md`：M0（Java 上传+状态机 ~0.5d）→ M1（Python 构建流水线 ~1d）→ M2（retrieve_context ~0.5d）→ M3（端到端+调优 ~0.5d），每 Task 含验证方式。
- 关键技术决策：Python 直连 PG 写 knowledge_chunks（需 DB_URL 注入 docker-compose）；embedding 用千问 text-embedding-v3（dim=1024）+ OpenAIEmbeddings；graph.json 落盘内存只读缓存保无状态；float8[] 余弦用 numpy，>5k chunk 再升级 pgvector。
- 下一步：用户确认计划 → 从 main 拉 feat/knowledge-graph-rag 分支 → 按 M0 开始。

## RAG 实施计划改为「接口驱动版」（用户调整策略）

- 用户否决"先全做完再测"的 M0-M3 顺序，改为：**先按接口开发，每个接口用输入→输出契约测试立即验证；graph_builder / vector_builder / retriever 三个 AI 组件各自独立 mock 测试**（因都依赖 LLM、输出不稳定）。
- plan 已重写为阶段 A-E：A 契约层（/build 端点→Java 3 端点→联通，均用桩）→ B 建图独立模块 → C embedding 独立模块 → D 检索独立模块 → E 端到端。spec 不变。
- 用户偏好确认：喜欢"接口驱动 + 分层独立测试"，讨厌依赖未就绪就堆代码。
