# 产品需求文档 (PRD)

## 1. 产品概述

**产品名称**：i-clothes 智能穿搭助手

**产品定位**：基于AI多模态分析的个性化穿搭推荐系统，用户上传参考照片即可获得专业的穿搭建议。

**目标用户**：
- 对穿搭有需求但缺乏灵感的普通用户
- 需要快速解决特定场景穿搭问题的用户

## 2. 核心功能

### MVP版本 (v0.1.0)

#### 2.1 照片上传
- 支持上传1-3张参考照片（场景照片、活动照片、风格参考）
- 支持JPG、PNG格式
- 单张照片限制：5MB以内

#### 2.2 文字描述（可选）
- 用户可补充文字说明，如"参加婚礼""日常通勤""约会"
- 字数限制：200字以内

#### 2.3 AI分析与推荐
**分析模型**：
- **千问（通义千问）**：主分析模型
  - 场景识别（正式/休闲/运动等）
  - 风格分析（简约/复古/街头等）
  - 色调提取
  - 氛围感知

**识别与生图模型**：
- **DeepSeek**：辅助模型
  - 识别照片中的具体单品
  - 可选：生成穿搭效果图

#### 2.4 推荐结果展示
- 文字穿搭建议：
  - 整体风格定位
  - 推荐色系
  - 单品建议（上衣、下装、鞋子、配饰）
  - 搭配技巧
- 可选：AI生成的穿搭效果图

## 3. 技术架构

### 3.1 技术栈
**后端**：
- Python 3.12（conda 环境，位于项目内 `.conda/`，不占用 C 盘）
- FastAPI（异步Web框架）
- **LangGraph**：agent 工作流框架，穿搭推荐以状态图（StateGraph）编排
- LangChain（`ChatOpenAI`）：统一模型接入层
- 千问API（阿里云通义千问，走 DashScope 的 OpenAI 兼容端点）
- DeepSeek API（后续版本）

**前端**：
- HTML5 + CSS3 + JavaScript（原生）
- 支持拖拽上传
- 响应式设计

**部署**：
- Docker容器化
- Nginx反向代理
- Linux服务器

### 3.2 系统架构
```
用户 → 前端界面 → FastAPI后端
                    ↓
              LangGraph 工作流 (StateGraph)
                    ↓
         ┌──────────┴──────────┐
     analyze_scene         [后续节点]
     (千问·场景分析)    identify_items / generate_outfit
                              (DeepSeek·识别/生图)
                    ↓
                返回推荐结果

所有模型客户端集中在 app/providers.py 构造，
节点只从 provider 获取实例，换模型只改一处。
```

### 3.3 后端目录结构
```
app/
  config.py          # 环境变量配置
  providers.py       # 集中式模型 provider（唯一构造模型处）
  main.py            # FastAPI 入口与接口
  graph/
    state.py         # 工作流共享状态（OutfitState）
    nodes.py         # 工作流节点（analyze_scene 等）
    workflow.py      # 图组装、编译、执行入口
frontend/            # 原生前端页面
```

## 4. 功能优先级

### P0 (必须实现 - MVP)
- [ ] 照片上传功能
- [ ] 千问API集成（场景分析）
- [ ] 基础推荐结果展示
- [ ] 部署到服务器

### P1 (第二版本)
- [ ] DeepSeek API集成（识别）
- [ ] 文字描述输入
- [ ] 效果图生成

### P2 (后续迭代)
- [ ] 季节适配（春夏秋冬）
- [ ] 主题穿搭（商务/约会/运动）
- [ ] 用户历史记录
- [ ] 收藏功能

## 5. 迭代计划

### v0.1.0 - MVP基础版（当前版本）
**目标**：实现最小可用产品
- 照片上传
- 千问分析
- 文字推荐结果
- 可部署

**预计时间**：2-3天

### v0.2.0 - 完整推荐
- 集成DeepSeek
- 添加生图功能
- 优化推荐逻辑

### v0.3.0 - 场景增强
- 季节判断
- 主题穿搭
- UI优化

## 6. 技术约束

- API调用成本控制
- 图片存储策略（临时存储，定期清理）
- 响应时间：< 10秒
- 并发支持：初期10个并发请求

## 7. 非功能需求

- **安全性**：API密钥安全存储（环境变量）
- **性能**：图片压缩，避免超大文件
- **可维护性**：代码模块化，日志完整
- **可扩展性**：模块化设计，便于添加新模型

## 8. 待确认问题

- [ ] 千问和DeepSeek的具体API版本和定价
- [ ] 服务器配置（CPU/内存/带宽）
- [ ] 是否需要用户登录系统

---

**文档版本**：v1.0  
**创建时间**：2026-08-11  
**负责人**：Claude (PM)  
**最后更新**：2026-08-11
