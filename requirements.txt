# i-clothes Python 依赖（与本地已验证环境版本一致；国内服务器部署时 pip 走 Dockerfile 里的阿里云镜像）
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
python-dotenv==1.2.2
pillow==11.1.0
langgraph==0.2.60
langchain-core==0.3.28
langchain-openai==0.2.14
langchain-text-splitters==0.3.2
networkx==3.6.1
# openai 不钉死上限：langchain-openai 0.2.14 要求 openai<2，交给 pip 解析到最新 1.x
openai>=1.61.0
httpx==0.28.1
# 追踪（arize-phoenix-otel）。Phoenix 服务器本体不安装：其强制依赖 pydantic-ai-slim[openai]→openai>=2.29，
# 与 langchain-openai 0.2.14（openai<2）物理冲突；main.py 检测到未安装会优雅跳过服务器启动
arize-phoenix-otel==0.17.1
# 知识库向量存储（版本与本地一致，Chroma 数据文件对版本敏感）
chromadb==1.5.9
